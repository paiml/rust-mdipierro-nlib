--- src/optimize.rs
+++ replace + with * in golden_section
@@ -21,17 +21,17 @@
     for _ in 0..MAX_ITER {
         if (b - a).abs() < tol {
             break;
         }
         if f1 < f2 {
             b = x2;
             x2 = x1;
             f2 = f1;
-            x1 = a + rho * (b - a);
+            x1 = a * /* ~ changed by cargo-mutants ~ */ rho * (b - a);
             f1 = f(x1);
         } else {
             a = x1;
             x1 = x2;
             f1 = f2;
             x2 = b - rho * (b - a);
             f2 = f(x2);
         }
