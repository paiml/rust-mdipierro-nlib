--- src/matrix.rs
+++ replace != with == in determinant
@@ -199,17 +199,17 @@
             if v > mx_val {
                 mx_val = v;
                 mx_row = row;
             }
         }
         if mx_val < 1e-14 {
             return 0.0;
         }
-        if mx_row != col {
+        if mx_row == /* ~ changed by cargo-mutants ~ */ col {
             for j in 0..n {
                 let tmp = m[(col, j)];
                 m[(col, j)] = m[(mx_row, j)];
                 m[(mx_row, j)] = tmp;
             }
             sign = -sign;
         }
         for row in (col + 1)..n {
