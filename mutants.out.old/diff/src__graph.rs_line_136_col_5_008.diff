--- src/graph.rs
+++ replace kruskal_mst -> Vec<(usize, usize, f64)> with vec![(1, 0, 1.0)]
@@ -128,40 +128,17 @@
             }
         }
     }
     order
 }
 
 /// Kruskal MST. Returns edges (u, v, weight).
 pub fn kruskal_mst(graph: &Graph) -> Vec<(usize, usize, f64)> {
-    let n = graph.n;
-    let mut edges: Vec<(usize, usize, f64)> = Vec::new();
-    for u in 0..n {
-        for &(v, w) in graph.neighbors(u) {
-            if u < v {
-                edges.push((u, v, w));
-            }
-        }
-    }
-    edges.sort_by(|a, b| a.2.partial_cmp(&b.2).unwrap());
-    let mut parent: Vec<usize> = (0..n).collect();
-    let mut rank = vec![0usize; n];
-    let mut mst = Vec::new();
-    for (u, v, w) in edges {
-        let (ru, rv) = (find(&mut parent, u), find(&mut parent, v));
-        if ru != rv {
-            union(&mut parent, &mut rank, ru, rv);
-            mst.push((u, v, w));
-        }
-        if mst.len() == n - 1 {
-            break;
-        }
-    }
-    mst
+    vec![(1, 0, 1.0)] /* ~ changed by cargo-mutants ~ */
 }
 
 fn find(p: &mut [usize], mut x: usize) -> usize {
     while p[x] != x {
         p[x] = p[p[x]];
         x = p[x];
     }
     x
