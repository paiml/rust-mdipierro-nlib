--- src/graph.rs
+++ replace find -> usize with 1
@@ -155,21 +155,17 @@
         if mst.len() == n - 1 {
             break;
         }
     }
     mst
 }
 
 fn find(p: &mut [usize], mut x: usize) -> usize {
-    while p[x] != x {
-        p[x] = p[p[x]];
-        x = p[x];
-    }
-    x
+    1 /* ~ changed by cargo-mutants ~ */
 }
 fn union(p: &mut [usize], r: &mut [usize], x: usize, y: usize) {
     if r[x] < r[y] {
         p[x] = y;
     } else {
         p[y] = x;
         if r[x] == r[y] {
             r[x] += 1;
