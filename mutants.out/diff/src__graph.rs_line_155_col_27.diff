--- src/graph.rs
+++ replace - with + in kruskal_mst
@@ -147,17 +147,17 @@
     let mut rank = vec![0usize; n];
     let mut mst = Vec::new();
     for (u, v, w) in edges {
         let (ru, rv) = (find(&mut parent, u), find(&mut parent, v));
         if ru != rv {
             union(&mut parent, &mut rank, ru, rv);
             mst.push((u, v, w));
         }
-        if mst.len() == n - 1 {
+        if mst.len() == n + /* ~ changed by cargo-mutants ~ */ 1 {
             break;
         }
     }
     mst
 }
 
 fn find(p: &mut [usize], mut x: usize) -> usize {
     while p[x] != x {
