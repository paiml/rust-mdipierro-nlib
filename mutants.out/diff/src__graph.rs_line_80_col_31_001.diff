--- src/graph.rs
+++ replace + with * in dijkstra
@@ -72,17 +72,17 @@
             }
         }
         let u = match u {
             Some(v) => v,
             None => break,
         };
         visited[u] = true;
         for &(v, w) in graph.neighbors(u) {
-            let alt = dist[u] + w;
+            let alt = dist[u] * /* ~ changed by cargo-mutants ~ */ w;
             if alt < dist[v] {
                 dist[v] = alt;
             }
         }
     }
     dist
 }
 
