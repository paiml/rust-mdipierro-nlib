--- src/optimize.rs
+++ replace < with > in newton_optimize
@@ -48,17 +48,17 @@
     tol: f64,
 ) -> f64 {
     for _ in 0..MAX_ITER {
         let g = df(x);
         if g.abs() < tol {
             return x;
         }
         let h = ddf(x);
-        if h.abs() < 1e-15 {
+        if h.abs() > /* ~ changed by cargo-mutants ~ */ 1e-15 {
             // Second derivative zero; cannot proceed with Newton step
             return x;
         }
         x -= g / h;
     }
     x
 }
 
